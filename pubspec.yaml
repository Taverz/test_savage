name: test_savage
description: "Test navigation system"
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  auto_route: any
    # path: auto_route_library-master/auto_route
  go_router: any
    # path: go_router
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  auto_route_generator: any
  build_runner: any

flutter:
  uses-material-design: true
